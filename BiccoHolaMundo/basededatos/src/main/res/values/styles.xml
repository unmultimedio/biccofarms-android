<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:listSelector">#ffff654f</item>
        <item name="android:listViewStyle">@style/lvs</item>
    </style>

    <style name="lvs" parent="Widget.AppCompat.ListView.Menu">
        <item name="android:listSelector">#ffff5f75</item>
    </style>

</resources>
